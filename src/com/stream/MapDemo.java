package com.stream;

import java.util.Arrays;
import java.util.List;
import java.util.Locale;
import java.util.stream.Collectors;

public class MapDemo {

    public static void main(String[] args) {

        //List<String> list= Arrays.asList("cars","parks","amusements","exhibitions");
       // list.stream().map(s->s.length()).collect(Collectors.toList()).forEach(System.out::println);
      //  list.stream().sorted().map(s->s.toUpperCase(Locale.ROOT)).forEach(System.out::println);

    }
}
